/** Max message body chars for demo chat persistence. */
export const CHAT_MESSAGE_TEXT_MAX_CHARS = 2000;

/** First page size for chat history; clients can page older messages as needed. */
export const CHAT_HISTORY_INITIAL_PAGE_SIZE = 50;

/** Hard upper bound accepted by the chatroom Durable Object history API. */
export const CHAT_HISTORY_MAX_PAGE_SIZE = 500;

{
	"name": "cf-starter-alchemy",
	"private": true,
	"type": "module",
	"exports": {
		".": {
			"types": "./password.ts",
			"default": "./password.ts"
		},
		"./worker-peer-scripts": {
			"types": "./worker-peer-scripts.ts",
			"default": "./worker-peer-scripts.ts"
		}
	},
	"scripts": {
		"deploy": "node -e \"process.exit(0)\"",
		"typegen:local": "tsgo --noEmit -p .",
		"typegen:prod": "tsgo --noEmit -p .",
		"typecheck:local": "tsgo --noEmit -p .",
		"typecheck:prod": "tsgo --noEmit -p ."
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"@typescript/native-preview": "catalog:",
		"typescript": "catalog:"
	}
}
